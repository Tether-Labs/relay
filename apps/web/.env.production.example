# Production values — copy to .env.production (gitignored)
# Used by `npm run build` locally and by `npm run deploy:web` to push build env to Vercel.
# Use pk_live_... / sk_live_... from Clerk (not test keys).

VITE_API_URL=https://relay-tether-labs.fly.dev
VITE_CLERK_PUBLISHABLE_KEY=pk_live_xxx
